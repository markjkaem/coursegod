import { component$ } from "@builder.io/qwik";

export default component$(() => {
   
    return (
      <>
       <div class="h-screen flex justify-center">
            <p>No subscription</p>
        </div>
      </>
    );
  });