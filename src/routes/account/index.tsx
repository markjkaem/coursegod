import { component$ } from "@builder.io/qwik";
import { useAuthSignin } from "../plugin@auth";
import { Form } from "@builder.io/qwik-city";
export default component$(() => {
  return (
    <>
      <div>account home, subscriptions, settings,</div>
    </>
  );
});
